# google-noai

A Firefox extension for disabling the AI Overview on all Google searches by
appending "-noai" to the search.

Every line is documented so anyone can follow along and see what exactly it's
doing.
